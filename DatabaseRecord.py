

class DatabaseRecord:
    def __init__(self, player, age, variant, numbered, is_autograph, is_relic, grader, grade, card_number, set, added_to_collection):
        self.player = player
        self.age = age
        self.variant = variant
        self.numbered = numbered
        self.is_autograph = is_autograph
        self.is_relic = is_relic
        self.grader = grader
        self.grade = grade
        self.card_number = card_number
        self.set = set
        self.added_to_collection = added_to_collection

